# Rust build output
/target

# Downloaded reference engines for benchmark matches (large binaries)
/bench_engines

# Local engine binaries and version snapshots (not source)
*.exe

# Trained NNUE nets and self-play training data (large / regenerable)
*.nnue
nnue_*.txt
data_*.txt
sf_*.txt
sf_*.txt.w*
datagen*_progress.txt
